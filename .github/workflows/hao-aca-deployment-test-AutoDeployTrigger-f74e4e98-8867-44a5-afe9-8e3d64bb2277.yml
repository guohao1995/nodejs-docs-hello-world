name: Trigger auto deployment for hao-aca-deployment-test

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/hao-aca-deployment-test-AutoDeployTrigger-f74e4e98-8867-44a5-afe9-8e3d64bb2277.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.HAOACADEPLOYMENTTEST_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.HAOACADEPLOYMENTTEST_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.HAOACADEPLOYMENTTEST_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: haoacr.azurecr.io
          registryUsername: ${{ secrets.HAOACADEPLOYMENTTEST_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.HAOACADEPLOYMENTTEST_REGISTRY_PASSWORD }}
          containerAppName: hao-aca-deployment-test
          resourceGroup: hao-ACA-test
          imageToBuild: haoacr.azurecr.io/hao-aca-deployment-test:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


